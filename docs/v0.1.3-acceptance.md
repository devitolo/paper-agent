# v0.1.3 acceptance candidate

Candidate source: `3e0f2b5`. Published app index:
`ghcr.io/devitolo/paper-agent@sha256:116e994bef7294767df08754c20870c596946a916e4e66f047efc973c858df13`.

The selected Ollama index remains
`docker.io/ollama/ollama@sha256:684d8674b4315fa18f4f0e973a118ec2652ed96f67563277839985175858e0ba`.

Validation: 247 tests passed, five private-fixture tests skipped (252 total).

On 2026-09-12 the isolated Mac arm64 run passed installation and model inference,
but arXiv returned HTTP 429 after waits of 60 and 120 seconds. Discovery failed;
extraction and downstream acceptance did not run. The 300-second extraction
allowance therefore remained unvalidated on macOS. Temporary Docker
resources were handled by the harness cleanup. Local evidence is retained in
`/private/tmp/paper-v013-acceptance/release-acceptance-results` until reviewed.

[Ubuntu acceptance run](https://github.com/devitolo/paper-agent/actions/runs/34706364444)
also failed because arXiv returned HTTP 429 after waits of 60 and 120 seconds.
The downloaded artifact confirms this in `scout-last.log`: all topic requests
failed and zero recommendations were produced. Model preparation succeeded;
three generation requests returned HTTP 200. No extraction timeout occurred in
this run because discovery never reached extraction. Both platforms were
blocked at live source discovery in those attempts. This evidence does
not establish the scope or duration of arXiv's rate limit.

## Successful Ubuntu rerun

[GitHub acceptance run 34719570823](https://github.com/devitolo/paper-agent/actions/runs/34719570823)
completed successfully on 2026-09-12 using the exact app and Ollama digests above.
The harness required fresh installation, model preparation and inference,
live arXiv discovery with persisted recommendations, HTTP feedback submission,
unchanged paper/recommendation/feedback counts after app recreation, and
abandoned Scout state reconciliation. This is automated HTTP/container acceptance,
not a visual browser review or an upgrade/restore test.

## Successful macOS rerun

On 2026-09-12, the same exact app/Ollama pair passed on Darwin arm64 in an
isolated temporary checkout on port 18126. The run lasted four minutes
(21:27:58–21:31:58 UTC), persisted 10 papers, three recommendations and one
feedback record, verified unchanged counts after app recreation, and reconciled
an abandoned Scout state to interrupted. The harness exited zero and recorded
`result=pass`. Extraction of recommended papers completed. Evidence is retained
locally in `/private/tmp/paper-v013-mac-final.yH9ESV/release-acceptance-results`.

Both target platforms now pass this automated fresh-install gate. Fresh-install
and workflow defaults select the verified v0.1.3 digest. Existing installation
records are preserved; this is not an upgrade/rollback or backup/restore gate,
nor proof that future arXiv requests cannot be throttled.
